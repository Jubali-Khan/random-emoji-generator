# Node.js Random Emoji Generator

Generate random emojis on the command line:

```bash
$ node index.js

```

Users can select emojis by name:

```bash
node index.js balloon

```

If the emoji doesn't exist, print an error:

```bash
node index.js balloons
balloons doesn\'t exist
```
